import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.tarnishedtracker.firebase',
  appName: 'TarnishedTracker',
  webDir: 'www'
};

export default config;
